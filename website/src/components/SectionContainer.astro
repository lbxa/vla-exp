---
export interface Props {
  id: string;
  title: string;
  class?: string;
}

const { id, title, class: className = "" } = Astro.props;
---

<section 
  id={id} 
  class={`mx-auto max-w-4xl px-md py-12 md:px-lg ${className}`}
>
  <div class="prose prose-lg prose-gray max-w-none">
    <slot />
  </div>
</section>

<style>
  /* Custom prose styles for academic content */
  .prose {
    @apply text-nerfies-gray-700 font-noto-sans;
  }
  
  .prose h1 {
    @apply text-4xl font-bold mb-8 text-nerfies-gray-800 font-google-sans border-b border-nerfies-gray-200 pb-4;
  }
  
  .prose h2 {
    @apply text-3xl font-semibold mb-6 mt-12 text-nerfies-gray-800 font-google-sans;
  }
  
  .prose h3 {
    @apply text-2xl font-semibold mb-4 mt-8 text-nerfies-gray-800 font-google-sans;
  }
  
  .prose h4 {
    @apply text-xl font-semibold mb-3 mt-6 text-nerfies-gray-800 font-google-sans;
  }
  
  .prose h5 {
    @apply text-lg font-semibold mb-2 mt-4 text-nerfies-gray-800 font-google-sans;
  }
  
  .prose h6 {
    @apply text-base font-semibold mb-2 mt-4 text-nerfies-gray-800 font-google-sans;
  }
  
  .prose p {
    @apply mb-4 leading-relaxed text-nerfies-gray-700;
  }
  
  .prose ul, .prose ol {
    @apply mb-4 pl-6;
  }
  
  .prose li {
    @apply mb-2 text-nerfies-gray-700;
  }
  
  .prose strong {
    @apply font-semibold text-nerfies-gray-800;
  }
  
  .prose em {
    @apply italic text-nerfies-gray-700;
  }
  
  .prose a {
    @apply text-nerfies-primary hover:text-nerfies-primary-hover no-underline hover:underline transition-colors duration-200;
  }
  
  .prose code {
    @apply bg-nerfies-gray-50 px-2 py-1 rounded text-sm font-mono text-nerfies-gray-800;
  }
  
  .prose pre {
    @apply bg-nerfies-gray-900 text-nerfies-gray-100 p-4 rounded overflow-x-auto mb-4;
  }
  
  .prose pre code {
    @apply bg-transparent p-0 text-nerfies-gray-100;
  }
  
  .prose blockquote {
    @apply border-l-4 border-nerfies-primary/30 pl-4 italic text-nerfies-gray-600 mb-4;
  }
  
  .prose hr {
    @apply border-nerfies-gray-300 my-8;
  }
  
  .prose table {
    @apply w-full border-collapse mb-4 min-w-max;
  }
  
  .prose th, .prose td {
    @apply border border-nerfies-gray-300 px-4 py-2 text-left;
  }
  
  .prose th {
    @apply bg-nerfies-gray-50 font-semibold;
  }
  
  /* Academic paper specific styles */
  .prose .math-display {
    @apply text-center my-6;
  }
  
  .prose .citation {
    @apply text-nerfies-primary text-sm;
  }
  
  .prose .author-info {
    @apply text-nerfies-gray-600 mb-4;
  }
  
  .prose .affiliation {
    @apply text-sm text-nerfies-gray-500 mb-6;
  }
  
  /* KaTeX styling */
  .prose .katex-display {
    @apply text-nerfies-gray-800 my-6 text-center;
    display: block;
    margin-left: auto;
    margin-right: auto;
  }
  
  .prose .katex {
    @apply text-nerfies-gray-800;
  }
  
  .prose .katex-display .katex {
    display: inline-block;
    text-align: center;
  }
  
  /* Override any prose styles that might interfere */
  .prose .katex-display .katex .base {
    display: inline-block;
  }
</style>