---
export interface Props {
  src: string;
  caption?: string;
  alt: string;
  width?: string;
  height?: string;
  class?: string;
}

const { 
  src, 
  caption, 
  alt, 
  width = "100%", 
  height = "auto",
  class: className = "" 
} = Astro.props;
---

<figure class={`my-xl ${className}`}>
  <div class="rounded border border-nerfies-gray-200 bg-nerfies-gray-50 p-md shadow-nerfies">
    <img 
      src={src} 
      alt={alt}
      width={width}
      height={height}
      class="h-auto w-full max-w-full rounded"
      loading="lazy"
    />
  </div>
  {caption && (
    <figcaption class="mt-3 text-center text-sm italic text-nerfies-gray-600">
      {caption}
    </figcaption>
  )}
</figure>

<style>
  figure {
    margin-left: auto;
    margin-right: auto;
  }
  
  img {
    /* No hover effects */
  }
  
  /* Responsive image container */
  @media (max-width: 768px) {
    figure {
      margin-left: -1rem;
      margin-right: -1rem;
    }
    
    .bg-nerfies-gray-50 {
      border-radius: 0;
      border-left: none;
      border-right: none;
    }
  }
</style>